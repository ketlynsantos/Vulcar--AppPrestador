<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appprestador">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppPrestador">
        <activity android:name=".Requests"></activity>
        <activity android:name=".ProfileEmployee" />
        <activity android:name=".MyAddress" />
        <activity android:name=".Profile" />
        <activity android:name=".Employee" />
        <activity android:name=".HomeEmployee" />
        <activity android:name=".Home" />
        <activity android:name=".Register2" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>